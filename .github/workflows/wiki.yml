name: Publish Wiki

on:
  workflow_run:
    workflows: ["CI"] # Name of the first workflow
    types:
      - completed

permissions:
  contents: write

jobs:
  build-wiki:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Git Identity
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Clone Wiki Repository
        run: |
          # The wiki is a separate git repository hosted alongside your main repo
          git clone https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.wiki.git wiki-repo

      - name: Update Wiki Content
        run: |
          # Clear existing wiki files
          rm -rf wiki-repo/*
          
          # Copy all files from the docs/ root (preserving .md extensions and images)
          for file in docs/*; do
            if [ -f "$file" ]; then
              filename=$(basename "$file")
              cp "$file" "wiki-repo/$filename"
            fi
          done

      - name: Push to Wiki
        run: |
          cd wiki-repo
          git add .
          git status
          git diff-index --quiet HEAD || git commit -m "Auto-update wiki from docs/ [skip ci]"
          git push origin master